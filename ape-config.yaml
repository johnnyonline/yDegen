networks:
  custom:
    - name: katana
      chain_id: 747474
      ecosystem: ethereum

node:
  ethereum:
    mainnet:
      # transaction_acceptance_timeout: 1800  # 30 minutes
      gas_limit:
        auto:
          multiplier: 5  # Multiply 2 times the result of eth_estimateGas
      uri: "${ETH_RPC_URL}"
      ws_uri: "${ETH_WS_URL}"
    katana:
      uri: "${KATANA_RPC_URL}"
      ws_uri: "${KATANA_WS_URL}"

  base:
    mainnet:
      # transaction_acceptance_timeout: 1800
      # gas_limit:
      #   auto:
      #     multiplier: 2
      uri: "${BASE_RPC_URL}"
      ws_uri: "${BASE_WS_URL}"

  arbitrum:
    mainnet:
      # transaction_acceptance_timeout: 1800
      # gas_limit:
      #   auto:
      #     multiplier: 2
      uri: "${ARB_RPC_URL}"
      ws_uri: "${ARB_WS_URL}"